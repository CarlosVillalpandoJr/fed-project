/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['static.wearpact.com', 'media.wearpact.com', 'pact.canto.com'],
    },
}

export default nextConfig 